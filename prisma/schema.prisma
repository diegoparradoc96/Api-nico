datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
  binaryTargets = ["native", "linux-musl-openssl-3.0.x"]
}

model Order {
  id    String   @id @default(uuid())
  date  DateTime
  time  String
  table Int
  total Float
}